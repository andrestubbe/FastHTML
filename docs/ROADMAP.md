# FastHTML Roadmap

- [x] Native AVX2 256-bit SIMD tag delimiter scanner.
- [x] Zero-copy off-heap memory sanitization (`FastPointer`).
- [x] Multi-tier safety profiles (`STRICT_WHITELIST`, `RELAXED`, `TEXT_ONLY`).
- [ ] AVX-512 vector acceleration kernels for ultra-large enterprise HTML streams.
- [ ] Streaming non-blocking chunk parser for `FastWebSpider` and `FastWebScrape`.
- [ ] Configurable dynamic tag & attribute whitelist builders.
